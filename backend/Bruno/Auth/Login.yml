info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}api/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "alexis@gmail.com",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
